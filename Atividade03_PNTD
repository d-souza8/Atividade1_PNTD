# Importação da biblioteca tkinter.
import tkinter as tk

# Importação da função de caixa de mensagem.
from tkinter import messagebox

# Função chamada quando o botão for clicado.
def mostrar_mensagem():
    messagebox.showinfo("Alerta!","Botão Clicado!")

# Criação da janela principal com título.
janela = tk.Tk()
janela.title("Atividade 3 - Janela com Imagem")

# Definição do tamanho da janela principal.
janela.geometry("450x300")

# Carga de imagem da janela principal (GIF ou PNG).
imagem = tk.PhotoImage(file="img_android.png")

# Criação do rótulo (Label) com a imagem.
rotulo = tk.Label(janela, image=imagem)

# Exibe o rótulo na janela.
rotulo.pack()

# O método pack() posiciona o rótulo na janela, com padding (espaçamento) vertical de 20px.
rotulo.pack(pady=20)

# Carga da imagem do botão.
imagem2 = tk.PhotoImage(file="img_click3.png")

# Cria um botão e associa a função mostrar_mmensagem à ação de clique no botão.
botao = tk.Button(janela, image=imagem2,command=mostrar_mensagem)

# Define o posicionamento do botão por coordenadas exatas de localização.
botao.place(x=190, y=240)

# Exibe e mantém a janela aberta até o usuário fechá-la.
janela.mainloop()
